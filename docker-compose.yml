version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: ofis_postgres
    environment:
      POSTGRES_DB: ofis_rezervasyon
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mysecretpassword
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: